@model IEnumerable<MachineServices.Models.RelevantCost>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RelevantCost1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SetDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Creators)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Service)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RelevantCost1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SetDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Creators.StaffId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Service.ServiceId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RelevantCostId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RelevantCostId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RelevantCostId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
