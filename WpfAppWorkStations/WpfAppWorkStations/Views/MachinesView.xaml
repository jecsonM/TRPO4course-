<UserControl x:Class="WpfAppWorkStations.Views.MachinesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель: список оборудования -->
        <Border Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Список оборудования" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   VerticalAlignment="Center"/>

                        <Button Grid.Column="1" 
                                Command="{Binding RefreshMachinesCommand}"
                                Content="🔄 Обновить"/>

                        <Button Grid.Column="1" 
                                Grid.Row="1"
                                Command="{Binding AddNewMachineCommand}"
                                Content="➕ Добавить"/>
                    </Grid>
                </Border>

                <!-- Список оборудования -->
                <ListBox 
                    Grid.Row="1" 
                    ItemsSource="{Binding MachinesViewModels}"
                    SelectedItem="{Binding CurrentlySelectedMachine}"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    HorizontalContentAlignment="Stretch">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border>
                                <Grid Margin="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" 
                                               Grid.Column="0" 
                                               Text="🔧 "/>
                                    <TextBlock Grid.Row="0" 
                                               Grid.Column="1" 
                                               Text="{Binding Machine.Model}"
                                               TextWrapping="Wrap"/>

                                    <TextBlock Grid.Row="1" 
                                               Grid.Column="0" 
                                               Text="#"/>
                                    <TextBlock Grid.Row="1" 
                                               Grid.Column="1" 
                                               Text="{Binding Machine.SerialNumber}"/>

                                    <TextBlock Grid.Row="2" 
                                               Grid.Column="0" 
                                               Text="🏢"/>
                                    <TextBlock Grid.Row="2" 
                                               Grid.Column="1" 
                                               Text="{Binding Machine.Client.CompanyName}"
                                               TextWrapping="Wrap"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Правая панель: детальная информация -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Background="{StaticResource MainBackBrush}">
            <Border Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок -->
                    <Border Grid.Row="0" Padding="15">
                        <StackPanel>
                            <TextBlock Text="{Binding CurrentlySelectedMachine.Machine.Model, StringFormat='Детальная информация об оборудовании: {0}'}" 
                                       FontSize="18" 
                                       FontWeight="Bold"/>
                            <TextBlock Text="{Binding CurrentlySelectedMachine.Machine.SerialNumber, StringFormat='Серийный номер: {0}'}"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <!-- Форма редактирования -->
                    <Grid Grid.Row="1" >
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0">
                                <StackPanel>
                                    <TextBlock Text="Компания-владелец"/>
                                    <ComboBox ItemsSource="{Binding ClientViewModels}"
                                              SelectedItem="{Binding CurrentlySelectedClient}"
                                              DisplayMemberPath="Client.CompanyName"
                                              IsEditable="True"
                                              IsTextSearchEnabled="True"/>

                                    <TextBlock Text="Модель оборудования"/>
                                    <TextBox Text="{Binding CurrentlySelectedMachine.Machine.Model, UpdateSourceTrigger=PropertyChanged}"
                                             TextWrapping="Wrap"/>

                                    <TextBlock Text="Серийный номер"/>
                                    <TextBox Text="{Binding CurrentlySelectedMachine.Machine.SerialNumber, UpdateSourceTrigger=PropertyChanged}"
                                             TextWrapping="Wrap"/>

                                    <TextBlock Text="Комментарий мастера"/>
                                    <TextBox Text="{Binding CurrentlySelectedMachine.Machine.MastersComment, UpdateSourceTrigger=PropertyChanged}"
                                             TextAlignment="Left"
                                             AcceptsReturn="True"
                                             MinHeight="100"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>

                    <!-- Кнопки действий -->
                    <StackPanel Grid.Row="3" 
            Orientation="Horizontal" 
            HorizontalAlignment="Right" 
            Margin="0,20,0,0">

                        <Button Command="{Binding SaveChangesCommand}"
            Content="💾 Сохранить изменения"
            IsEnabled="{Binding CurrentlySelectedMachine.IsChanged}"/>

                        <Button Command="{Binding CancelChangesCommand}"
            Content="❌ Отменить изменения"
            IsEnabled="{Binding CurrentlySelectedMachine.IsChanged}"/>

                        
            
                    </StackPanel>
                    <Button Grid.Row="2"
                        Command="{Binding DeleteMachineCommand}"
                            HorizontalAlignment="Right"
                            Content="🗑️ Удалить оборудование"
                            Background="#DC3545"
                            Foreground="White"
                            Margin="10,0,0,0"
                            Visibility="{Binding CurrentlySelectedMachine}"/>
                </Grid>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>