<UserControl x:Class="WpfAppWorkStations.Views.AuthorizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfAppWorkStations.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="340"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MaxHeight="30"/>
            <RowDefinition Height="12*"/>
        </Grid.RowDefinitions>
        <Border 
            VerticalAlignment="Top" 
            BorderThickness="3" 
            Grid.Column="1" 
            Grid.Row="1"
            Background="{StaticResource MainBackBrush}" MaxHeight="350">
            <Grid Margin="40 20 40 0">
                <StackPanel>
                    <TextBlock Text="Авторизация"  FontSize="28" HorizontalAlignment="Center" Margin="0 0 0 30"/>
                    <TextBlock >Логин</TextBlock>
                    <TextBox Text="2" BorderBrush="Transparent" Margin="0 0 0 30"></TextBox>
                    <TextBlock>Пароль</TextBlock>
                    <PasswordBox 
                        Name="PasswordBox" 
                        PasswordChar="*" 
                        BorderBrush="Transparent" 
                        Background="{StaticResource LightBackBrush}" 
                        Margin="0 0 0 30"
                        
                        ></PasswordBox>
                    <Button Content="Вход" Margin="0 0 0 30"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
